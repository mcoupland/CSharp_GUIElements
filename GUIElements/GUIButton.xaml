<UserControl x:Class="GUIElements.GUIButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUIElements"
             mc:Ignorable="d" Margin="2" BorderBrush="Black" BorderThickness="0"
             VerticalAlignment="Center" HorizontalAlignment="Center">
    <Control.Background>
        <ImageBrush x:Name="BackgroundBrush" x:FieldModifier="public" ></ImageBrush>
    </Control.Background>
    <Grid>
        <Border Name="guiFrame"
                Grid.Row="0" Grid.Column="0" 
                Background="Black" Opacity="0.4"
                BorderBrush="Transparent" BorderThickness="0"></Border>
        <Border Name="guiBorder"
                Grid.Row="0" Grid.Column="0" 
                Background="Transparent"
                BorderBrush="Transparent" BorderThickness="0"
                Margin="0">
            <DockPanel Name="guiPanel" 
                       Margin="0" Background="White"
                       VerticalAlignment="Center" HorizontalAlignment="Center">
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
